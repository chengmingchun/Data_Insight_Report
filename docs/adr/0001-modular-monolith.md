# ADR-0001：采用模块化单体

- 状态：已接受
- 日期：2026-07-15

## 决策

采用 Streamlit 作为展示层，以 `AnalysisOrchestrator` 作为门面，加载、清洗、统计、异常、图表、洞察和报告各自独立。

## 原因

原型需要单命令运行，同时要求核心模块可独立测试。模块化单体能保留边界和替换能力，并避免服务拆分带来的部署成本。

## 后果

页面不得包含业务计算；模块间通过 Pydantic DTO 和明确的数据帧契约传递结果。
