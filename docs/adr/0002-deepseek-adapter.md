# ADR-0002：DeepSeek 通过兼容接口接入

- 状态：已接受
- 日期：2026-07-15

## 决策

默认 Provider 为 DeepSeek，使用 OpenAI-compatible 客户端适配器。Provider 工厂负责在远程模型和模板降级之间选择。

## 原因

这样可以满足默认厂商要求，同时保持业务层不依赖具体 SDK。测试可注入 Mock Provider，外部服务失败不影响报告主链路。

## 安全约束

密钥只在前端密码控件和当前会话中传递，不进入环境样例以外的文件、不进入日志、不进入报告。
